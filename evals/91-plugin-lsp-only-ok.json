{
  "id": "91-plugin-lsp-only-ok",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/plugin-lsp-only",
    "needs_home_override": true,
    "scanners": ["scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [],
    "must_not_flag": [
      "PLUGIN-MISSING-MANIFEST",
      "PLUGIN-BROKEN-REF",
      "PLUGIN-DISABLED"
    ],
    "expect_clean": true
  },
  "expected_behavior": [
    "the install dir holds only README.md — no plugin.json, no skills/commands/agents dir, no .mcp.json",
    "its marketplace catalog entry declares the whole capability set inline (lspServers + \"strict\": false), which is the upstream shape for an LSP-only plugin",
    "scan-graph.sh therefore does NOT emit PLUGIN-MISSING-MANIFEST (false-positive counterpart to case 92)"
  ]
}
