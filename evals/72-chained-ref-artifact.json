{
  "id": "72-chained-ref-artifact",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/chained-ref-artifact",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [],
    "must_not_flag": ["CHAINED-REF"],
    "expect_clean": false
  },
  "expected_behavior": [
    "no CHAINED-REF: `.claude/operation-completed.flag` and `.claude/runner.local.md` are single-file project artifacts (no nested subtree), not links into another skill's tree. A nested cross-skill ref like .claude/skills/other/SKILL.md would still flag (case 20)."
  ]
}
