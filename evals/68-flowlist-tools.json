{
  "id": "68-flowlist-tools",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/flowlist-tools",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "AGENT-BAD-SCHEMA", "path_substring": "flowagent" } ],
    "must_not_flag": ["BAD-FRONTMATTER-SCHEMA", "UNKNOWN-FRONTMATTER-FIELD"],
    "expect_clean": false
  },
  "expected_behavior": [
    "skill allowed-tools as a quoted flow-list ([Read, Grep, \"Bash(npm test:*)\"]) is accepted (docs: 'or a YAML list') — no BAD-FRONTMATTER-SCHEMA; the subagent tools flow-list ([Read, Write]) IS flagged AGENT-BAD-SCHEMA with the reworded 'inline YAML flow-list' message (undocumented for subagent files)"
  ]
}
