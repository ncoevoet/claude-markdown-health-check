{
  "id": "07-invalid-json",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/invalid-json",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "INVALID-JSON", "path_substring": "settings.json" } ],
    "must_not_flag": [],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [INVALID-JSON] for settings.json with a trailing comma"
  ]
}
