{
  "id": "93-claudemd-npm-placeholder",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/claudemd-npm-placeholder",
    "needs_home_override": true,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [],
    "must_not_flag": ["CLAUDEMD-DEAD-SCRIPT"],
    "expect_clean": true
  },
  "expected_behavior": [
    "`npm run build:[project]` truncates to the stub `build:` at the `[` boundary — a placeholder, not a script name",
    "`npm run oss|istra|inf|common:test` truncates to `oss` at the `|` boundary",
    "`npm run test:*` / `npm run oss:test:*` inside permission patterns truncate to `test:` / `oss:test:` at the `*` boundary",
    "`npm run <app>:start:<profile>` is skipped by the existing `<` guard",
    "the two real scripts resolve, so the tree is clean"
  ]
}
