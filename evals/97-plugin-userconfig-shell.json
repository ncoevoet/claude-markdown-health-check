{
  "id": "97-plugin-userconfig-shell",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/plugin-userconfig-shell",
    "needs_home_override": false,
    "scanners": ["scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [
      { "tag": "PLUGIN-USERCONFIG-IN-SHELL", "path_substring": "plugin.json" },
      { "tag": "PLUGIN-USERCONFIG-IN-SHELL", "path_substring": "monitors.json" }
    ],
    "must_not_flag": ["PLUGIN-ABS-PATH", "MCP-PLAINTEXT-SECRET", "MCP-BAD-DEF"],
    "expect_clean": false
  },
  "expected_behavior": [
    "scan-graph.sh emits [PLUGIN-USERCONFIG-IN-SHELL] for the hook command and for the monitor command, where Claude Code rejects ${user_config.…} substitution",
    "the same interpolation inside the MCP server env block is supported and is NOT flagged"
  ]
}
