{
  "id": "79-mcp-plaintext-secret",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/mcp-plaintext-secret",
    "needs_home_override": false,
    "scanners": ["scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "MCP-PLAINTEXT-SECRET", "path_substring": "mcp" } ],
    "must_not_flag": ["MCP-BAD-DEF"],
    "expect_clean": false
  },
  "expected_behavior": [
    "scan-graph.sh emits [MCP-PLAINTEXT-SECRET] for the 'db' server whose env.API_KEY embeds a literal sk- credential",
    "the server has a command, so it is NOT flagged MCP-BAD-DEF"
  ]
}
