{
  "id": "56-hook-env-leak",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/hook-env-leak",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "HOOK-ENV-LEAK", "path_substring": "settings" } ],
    "must_not_flag": ["HOOK-NO-SHEBANG", "SUSPICIOUS-TIMEOUT"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [HOOK-ENV-LEAK] because the http hook carries an Authorization header but sets neither allowedEnvVars nor httpHookAllowedEnvVars, so the whole environment is forwarded"
  ]
}
