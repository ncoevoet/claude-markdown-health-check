{
  "id": "102-plugin-default-enabled",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/plugin-default-enabled",
    "needs_home_override": true,
    "scanners": ["scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [],
    "must_not_flag": ["PLUGIN-DISABLED", "PLUGIN-VERSION-DRIFT", "PLUGIN-MISSING-MANIFEST"],
    "expect_clean": false
  },
  "expected_behavior": [
    "parked@demo-market is installed but absent from enabledPlugins, yet its manifest sets defaultEnabled:false — the plugin ships parked by design, so no PLUGIN-DISABLED is raised"
  ]
}
