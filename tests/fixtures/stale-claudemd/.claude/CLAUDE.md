# Checker service — Claude guide

A small lint service. Use the command below.

## Commands
- Lint the project: `bash skills/checker/lint.sh`

## Architecture
- `skills/checker/` — the lint skill and its entry script
