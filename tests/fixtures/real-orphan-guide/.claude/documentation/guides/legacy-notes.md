# Legacy migration notes

Scratch notes from an old migration. Nothing in this config references this file —
no CLAUDE.md, no settings.json, no skill points at it. It is a genuine orphan and
SHOULD be flagged, because the inbound-reference grep returns zero.

- Old endpoint shapes (pre-v2).
- A checklist that no longer applies.
