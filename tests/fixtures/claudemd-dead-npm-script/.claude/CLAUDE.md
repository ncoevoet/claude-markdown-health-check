# Demo app — Claude guide

A small Angular workspace. Use the commands below.

## Commands
- First setup: `npm run zod:install`
- Build the app: `npm run build:oss`
- Regenerate i18n: `npm run oss:lang`

## Architecture
- `src/` — application source
- `projects/ngx-zod-models/` — the zod model library
