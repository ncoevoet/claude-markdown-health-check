# Demo service — Claude guide

A small service. Build with `make build`, test with `make test`, run with `make run`.

## Conventions
See `.claude/rules/conventions.md` for all 5 rules with examples.

## Architecture
- `src/` — service source
- `.claude/rules/` — the coding conventions
