{
  "id": "23-duplicate-entry",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/duplicate-entry",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "DUPLICATE-ENTRY", "path_substring": "settings.json" } ],
    "must_not_flag": ["INVALID-JSON"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [DUPLICATE-ENTRY] for the repeated Bash(ls:*) entry in permissions.allow"
  ]
}
