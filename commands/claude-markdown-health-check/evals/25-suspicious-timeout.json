{
  "id": "25-suspicious-timeout",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/suspicious-timeout",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "SUSPICIOUS-TIMEOUT", "path_substring": "settings.json" } ],
    "must_not_flag": ["INVALID-JSON"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [SUSPICIOUS-TIMEOUT] for a command hook with timeout 2000s (>2x the 600s default)"
  ]
}
