{
  "id": "49-agent-bypass-perms",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/agent-bypass-perms",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "AGENT-BYPASS-PERMS", "path_substring": "deployer" } ],
    "must_not_flag": ["AGENT-BAD-SCHEMA"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [AGENT-BYPASS-PERMS] because permissionMode is bypassPermissions (a valid enum value, but it disables every permission prompt for the agent); no AGENT-BAD-SCHEMA because the value is well-formed"
  ]
}
