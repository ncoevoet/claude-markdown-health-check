{
  "id": "32-rule-oversized",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/rule-oversized",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "RULE-OVERSIZED", "path_substring": "big.md" } ],
    "must_not_flag": ["BAD-RULE-FRONTMATTER"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [RULE-OVERSIZED] for a >200-line rule with no paths: scope (loaded into every session)"
  ]
}
