{
  "id": "35-secret-word-boundary",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/secret-word-boundary",
    "needs_home_override": false,
    "scanners": ["validate-skills", "scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [],
    "must_not_flag": ["EMBEDDED-SECRET"],
    "expect_clean": true
  },
  "expected_behavior": [
    "a markdown TOC with anchors like #task-type-a-create-from-scratch (where the 'sk' is mid-word in 'task') is NOT flagged EMBEDDED-SECRET — the sk-/AKIA/etc. patterns now require a left word boundary (locks a real-world false positive where a word ending in 'sk' precedes a hyphenated phrase)"
  ]
}
