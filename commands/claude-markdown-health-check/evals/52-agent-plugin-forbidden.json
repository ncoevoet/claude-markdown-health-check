{
  "id": "52-agent-plugin-forbidden",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/agent-plugin-forbidden",
    "needs_home_override": false,
    "scanners": ["validate-skills"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [ { "tag": "AGENT-PLUGIN-FORBIDDEN-FIELD", "path_substring": "helper" } ],
    "must_not_flag": ["AGENT-BYPASS-PERMS", "AGENT-BAD-SCHEMA"],
    "expect_clean": false
  },
  "expected_behavior": [
    "validate-skills.sh emits [AGENT-PLUGIN-FORBIDDEN-FIELD] because the agent lives in a plugin tree (.claude-plugin/plugin.json present) yet declares permissionMode, which plugin agents silently ignore"
  ]
}
