{
  "id": "95-plugin-path-keys",
  "command": "claude-markdown-health-check",
  "fixture": {
    "kind": "claude-tree",
    "dir": "tests/fixtures/plugin-path-keys",
    "needs_home_override": false,
    "scanners": ["scan-graph"]
  },
  "grader": { "method": "code" },
  "success_criteria": {
    "must_detect": [
      { "tag": "PLUGIN-ABS-PATH", "path_substring": "plugin.json" },
      { "tag": "PLUGIN-MISPLACED-DIR", "path_substring": "workflows" }
    ],
    "must_not_flag": ["PLUGIN-BAD-VERSION"],
    "expect_clean": false
  },
  "expected_behavior": [
    "scan-graph.sh emits [PLUGIN-ABS-PATH] for the workflows and experimental.themes paths, which are declared without the required ./ prefix",
    "scan-graph.sh emits [PLUGIN-MISPLACED-DIR] for the workflows/ dir nested inside .claude-plugin/",
    "the documented skills: \".\" exception is NOT flagged"
  ]
}
